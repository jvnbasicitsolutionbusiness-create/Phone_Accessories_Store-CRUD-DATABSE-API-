const session=JSON.parse(sessionStorage.getItem("stockflow_session")||"null");if(!session){location.href="login.html"}else{const name=document.getElementById("dashUser");if(name)name.textContent=session.fullName||session.username}
const menuBtn=document.getElementById("menuBtn"),sidebar=document.getElementById("sidebar"),overlay=document.getElementById("sidebarOverlay");function closeMenu(){sidebar?.classList.remove("open");overlay?.classList.remove("show")}menuBtn?.addEventListener("click",()=>{sidebar.classList.add("open");overlay.classList.add("show")});overlay?.addEventListener("click",closeMenu);document.querySelectorAll(".sidebar-nav a").forEach(a=>a.addEventListener("click",e=>{if(window.innerWidth<760){e.preventDefault();closeMenu()}}));
document.getElementById("logoutBtn")?.addEventListener("click",()=>{if(confirm("Are you sure you want to log out?")){sessionStorage.removeItem("stockflow_session");location.href="login.html"}});